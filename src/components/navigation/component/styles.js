export default function styles(theme) {
    return {
        root: {
            border: `solid ${theme.palette.success.main}`,
            borderRadius: '15%',
            display: 'flex',
            justifyContent: 'center'
        }
    }
}
