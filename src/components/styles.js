export default function styles(theme) {
    return {
        root: {
            backgroundColor: theme.palette.grey[300],
            height: '98vh'
        },
        row: {
            display: 'flex',
            marginTop: theme.spacing(4),
            height: '70%'
        }
    }
}
